[package]
name = "simple_bytebuf"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]

[build-dependencies]
cbindgen = "0.28.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
strip = true
debug = false